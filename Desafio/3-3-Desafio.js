let numero = prompt('Digite um número para contagem regressiva.');
let contador = numero; // Inicializa a variável 'contador' com o valor de 'numero'

while (contador >= 0) { // Ajusta a condição de parada
    console.log(contador); // Mostra o valor atual de 'contador'
    contador--; // Decrementa 'contador'
}
